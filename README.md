# Causal KPI Dashboard (LLM + Data Hybrid)

This dashboard visualizes KPI relationships using:
- LLM-derived causal contributors
- Validated time-series insights

## Run Locally
```bash
pip install -r requirements.txt
streamlit run app.py

